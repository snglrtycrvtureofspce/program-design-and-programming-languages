<Window x:Class="Lab25.Task3Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab25"
        mc:Ignorable="d"
        Title="Task3Window" Height="500" Width="900" Closing="Window_Closing" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid x:Name="matrixGrid" Margin="5" Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="1" Binding="{Binding Path=[0]}" Width="*" />
                <DataGridTextColumn Header="2" Binding="{Binding Path=[1]}" Width="*" />
                <DataGridTextColumn Header="3" Binding="{Binding Path=[2]}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" Margin="5" Grid.Row="1">
            <Button Content="Вычислить" Click="CalculateButton_Click" Margin="5" />
            <Button Content="Сбросить" Click="ResetButton_Click" Margin="5" />
        </StackPanel>
    </Grid>
</Window>
